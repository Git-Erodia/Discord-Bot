const {MessageEmbed} = require("discord.js");
const Raid = require("../databases/Raid");
module.exports = {
    name: 'guildBanRemove\n',

    async execute(ban)
    {
        console.log(ban)
    },
};
